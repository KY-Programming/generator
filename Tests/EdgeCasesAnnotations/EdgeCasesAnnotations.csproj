<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ProjectGuid>{0F0BD27F-B1E8-4BA0-BB6E-C0F7DFEF979B}</ProjectGuid>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <DocumentationFile>bin\Release\EdgeCasesAnnotations.xml</DocumentationFile>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <DocumentationFile>bin\Debug\EdgeCasesAnnotations.xml</DocumentationFile>
    </PropertyGroup>

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="KY.Generator" Version="10.0.1-preview.13" />
        <PackageReference Include="KY.Generator.Angular" Version="10.0.1-preview.13" />
        <PackageReference Include="KY.Generator.AspDotNet" Version="10.0.1-preview.13" />
        <PackageReference Include="KY.Generator.Reflection" Version="10.0.1-preview.13" />
        <!-- Only for the NugetPackageDependency scenario: the loader has to resolve it to read the type. -->
        <PackageReference Include="System.Reactive" Version="6.0.1" />
    </ItemGroup>

</Project>
