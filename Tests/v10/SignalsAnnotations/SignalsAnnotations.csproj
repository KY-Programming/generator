<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net9.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>Signals</RootNamespace>
      <ProjectGuid>{d5d865ec-d536-4507-ba20-bc9f405af26e}</ProjectGuid>
  </PropertyGroup>

    <!--
        TEMPORARY: project references instead of the KY.Generator package references below. The GenerateWithSignals
        annotation does not exist in any packed version yet, so the package can not compile this project. Replace both
        project reference groups with the package references as soon as a package containing GenerateWithSignals is packed
        and run the generation through the MSBuild targets again like every other v10 test.

        <PackageReference Include="KY.Generator" Version="10.0.0-preview.47" />
        <PackageReference Include="KY.Generator.Reflection" Version="10.0.0-preview.47" />
    -->
    <ItemGroup>
        <ProjectReference Include="..\..\..\Angular\KY.Generator.Angular.csproj" />
        <ProjectReference Include="..\..\..\Angular.Generator\KY.Generator.Angular.Generator.csproj" />
        <ProjectReference Include="..\..\..\Reflection\KY.Generator.Reflection.csproj" />
        <ProjectReference Include="..\..\..\Reflection.Generator\KY.Generator.Reflection.Generator.csproj" />
    </ItemGroup>

    <ItemGroup>
        <Folder Include="Output\" />
    </ItemGroup>

</Project>
