<!--------------------------------------------------------------------------
 <auto-generated>
     This code was generated with KY.Generator.Builder 1.0.0.0

     Manual changes to this file may cause unexpected behavior in your application.
     Manual changes to this file will be overwritten if the code is regenerated.
 </auto-generated>
-------------------------------------------------------------------------->

# FromDatabaseToTypeScript

Demonstrates generating TypeScript models from the schema of a T-SQL database - the same database
`FromDatabaseToCsharp` reads, only the writer differs.

The server is not part of the repository: `prepare.js` creates it from `../Shared/docker-compose.yml` and
applies `../Shared/schema.sql` before anything is built, `validate.js` type-checks the generated TypeScript
afterwards, and `cleanup.js` removes the container again. The container belongs to the run, so every build
starts from an empty server and nothing is left on the machine. Both files are shared with
`FromDatabaseToCsharp` and are listed as solution items, so they show up in either solution.

Both T-SQL examples use the same container name, so they must not run at the same time - hence the
explicit order that keeps them apart.

## Order

101

## Parameters

```
readid -solution=$\Examples\Tsql\FromDatabaseToTypeScript.sln -project=$\Examples\Tsql\FromDatabaseToTypeScript\FromDatabaseToTypeScript.csproj msbuild set -output=$\Examples\Tsql\FromDatabaseToTypeScript\ load -assembly=$\Examples\Tsql\FromDatabaseToTypeScript\bin\Debug\net10.0\FromDatabaseToTypeScript.dll fluent annotation
```

## Preparation

- prepare.js `passed, 2 files validated`

## Output

- Output/
    - address.ts `#a47696b7`
    - index.ts `#a29b03d6`
    - person.ts `#3f43d0e0`

## Validation

- validate.js `passed, 3 files validated`

## Cleanup

- cleanup.js `passed, 0 files validated`

## Status

- **Last Build:** 2026-08-09 11:32:33
- **Duration:** 13.8s
- **Status:** Warning
- **Info:** 2 changed
- **Last Success:** 2026-08-09 11:32:33
- **Generator:** 10.0.1-preview.11
