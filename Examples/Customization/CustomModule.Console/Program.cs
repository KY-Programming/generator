// The generated Output/Greeter.cs is compiled into this project, so a custom module that emits C# which
// does not compile breaks this build. It is not called from here on purpose: the generated file is
// deleted before a verification build and only written again afterwards, so code depending on it would
// not compile on the first pass.

System.Console.WriteLine("Nothing to do here - see Output/ for what the custom module generated.");
